generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model item {
  item_id   Int    @id @default(autoincrement())
  item_name String
}
